loopback_users = none

log.console = true
log.console.level = error

log.file.level = debug
log.file.rotation.date = $D00
# log.file.rotation.date = $H30
# log.file.rotation.size = 1024
log.file.rotation.count = 50

log.exchange = false
log.exchange.level = info
